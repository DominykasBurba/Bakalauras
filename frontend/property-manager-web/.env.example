# API base (must end up as .../api for routes).
# Recommended for local dev: same-origin proxy (see vite.config.ts) — avoids many "Failed to fetch" issues:
# VITE_API_URL=/api
# Or call the API directly:
VITE_API_URL=http://localhost:5076/api
